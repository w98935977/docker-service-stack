# Copy this file to .env before first launch:
#   cp .env.example .env
#
# POSTGRES_PASSWORD is intentionally blank.
# Set a strong password before running docker compose up.

APP_NAME=docker-service-stack
POSTGRES_DB=service_db
POSTGRES_USER=service_user
POSTGRES_PASSWORD=
