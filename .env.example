# Copy this file to .env before first launch:
#   cp .env.example .env

APP_NAME=docker-service-stack
POSTGRES_DB=service_db
POSTGRES_USER=service_user
POSTGRES_PASSWORD=replace_with_a_strong_password
